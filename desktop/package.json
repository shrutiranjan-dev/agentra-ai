{
  "name": "@assistant/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && electron ./dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@assistant/shared": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.8.2",
    "electron": "^33.2.1",
    "typescript": "^5.6.3"
  }
}
